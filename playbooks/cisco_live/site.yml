---
# Cisco Live demo — run all phases in order (or use --tags)
#
#   ansible-playbook playbooks/cisco_live/site.yml -i inventory/cisco_live_hosts.yml
#   ansible-playbook playbooks/cisco_live/site.yml -i inventory/cisco_live_hosts.yml --tags phase1

- name: Import phase playbooks
  ansible.builtin.import_playbook: 03_ios_interfaces_gather.yml
  tags: [phase3, ios]

- name: Import phase playbooks
  ansible.builtin.import_playbook: 04_parse_and_compare_interfaces.yml
  tags: [phase4, ios]

